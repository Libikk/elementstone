import styles from './Footer.module.scss';

const Footer: React.FC = () => {
    return <footer className={styles.footer}>
        <span className={styles.footerText}>Elmstone Systems Ltd.</span>
    </footer>
}

export default Footer;
